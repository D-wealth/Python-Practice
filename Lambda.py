add = lambda num1, num2: num1 + num2

number1 = 3
number2 = 6

print(add(number1, number2))


# LAMBDA EXPRESSIONS IN PYTHON
# add two numbers using LAMBDA expressions

add_numbers = lambda num1, num2: num1 + num2
print(add_numbers(3,4))

# MAIN LAMBDA EXPRESSIONS
result = map(lambda x:x**2, [4, 6, 9, 76, 45])
print(list(result))

# Note : LAMBDA EXPRESSIONS IS JUST A LINE EXPRESSION
